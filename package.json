{
  "name": "smartretail",
  "version": "1.0.0",
  "description": "project for storing postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run SmartRetail.postman_collection.json --reporters cli.junit --reporter-junit-export newman.xml"
  },
  "author": "Christian Teguh",
  "dependencies": {
    "newman": "^4.5.7",
    "reporter": "^0.1.0"
  },
  "devDependencies": {
    "karma-junit-reporter": "^2.0.1"
  }
}
